---
phase: 08-header-footer-presets-and-pre-header
plan: "08-00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/client/src/components/builder/__tests__/HeaderPresetSlot.test.tsx
  - apps/client/src/components/builder/__tests__/FooterPresetSlot.test.tsx
  - apps/client/src/components/builder/__tests__/PresetSelector.test.tsx
  - apps/client/src/store/__tests__/useNewsletterStore.test.ts
autonomous: true
requirements:
  - HF-01
  - HF-02
  - HF-03
  - HF-04

must_haves:
  truths:
    - "4 new test files / test additions exist; 3 are RED with 'Cannot find module' errors"
    - "3 store tests are RED because updateHeader/updateFooter/updatePreHeader actions do not exist yet"
    - "The existing 105 tests continue to pass (GREEN)"
  artifacts:
    - path: "apps/client/src/components/builder/__tests__/HeaderPresetSlot.test.tsx"
      provides: "RED tests for HeaderPresetSlot (4 tests — import fails)"
      contains: "HeaderPresetSlot"
    - path: "apps/client/src/components/builder/__tests__/FooterPresetSlot.test.tsx"
      provides: "RED tests for FooterPresetSlot (3 tests — import fails)"
      contains: "FooterPresetSlot"
    - path: "apps/client/src/components/builder/__tests__/PresetSelector.test.tsx"
      provides: "RED tests for PresetSelector dialog (4 tests — import fails)"
      contains: "PresetSelector"
  key_links:
    - from: "apps/client/src/components/builder/__tests__/HeaderPresetSlot.test.tsx"
      to: "apps/client/src/components/builder/HeaderPresetSlot.tsx"
      via: "import { HeaderPresetSlot } from '../HeaderPresetSlot'"
      pattern: "HeaderPresetSlot"
    - from: "apps/client/src/store/__tests__/useNewsletterStore.test.ts"
      to: "apps/client/src/store/useNewsletterStore.ts"
      via: "useNewsletterStore.getState() as any"
      pattern: "updateHeader|updateFooter|updatePreHeader"
---

<objective>
Create TDD RED stubs for all Phase 8 components and store actions. These tests intentionally fail
because the modules they import do not exist yet. They will turn GREEN as implementation plans
08-03 through 08-06 execute.

Purpose: Establish a clear test contract for each Phase 8 deliverable before any implementation.
Output: 3 new test files (RED: module-not-found) + 1 store test file updated with 3 RED tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-header-footer-presets-and-pre-header/08-RESEARCH.md
@apps/client/src/store/__tests__/useNewsletterStore.test.ts
@apps/client/src/fixtures/newsletter.fixture.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeaderPresetSlot.test.tsx and FooterPresetSlot.test.tsx</name>
  <files>
    apps/client/src/components/builder/__tests__/HeaderPresetSlot.test.tsx
    apps/client/src/components/builder/__tests__/FooterPresetSlot.test.tsx
  </files>
  <action>
Create `apps/client/src/components/builder/__tests__/HeaderPresetSlot.test.tsx` with this exact content:

```typescript
// HeaderPresetSlot component tests — Phase 8 TDD RED stubs
// RED: Cannot find module '../HeaderPresetSlot' — component is created in Plan 08-04.
// These tests turn GREEN after Plan 08-04 executes.
import React from 'react';
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { HeaderPresetSlot } from '../HeaderPresetSlot';

function makeWrapper() {
  const client = new QueryClient({
    defaultOptions: { queries: { retry: false, staleTime: Infinity } },
  });
  return function Wrapper({ children }: { children: React.ReactNode }) {
    return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
  };
}

describe('HeaderPresetSlot (HF-01)', () => {
  it('renders "No header selected" when presetId is empty string', () => {
    const { getByText } = render(<HeaderPresetSlot presetId="" />, { wrapper: makeWrapper() });
    expect(getByText('No header selected')).toBeInTheDocument();
  });

  it('renders "No header selected" when presetId is unknown (no matching preset fetched)', () => {
    const { getByText } = render(<HeaderPresetSlot presetId="unknown-xyz" />, { wrapper: makeWrapper() });
    expect(getByText('No header selected')).toBeInTheDocument();
  });

  it('renders a div element wrapping the slot area', () => {
    const { container } = render(<HeaderPresetSlot presetId="" />, { wrapper: makeWrapper() });
    expect(container.querySelector('div')).toBeInTheDocument();
  });

  it('accepts presetId prop without throwing', () => {
    expect(() => {
      render(<HeaderPresetSlot presetId="header-minimal-logo" />, { wrapper: makeWrapper() });
    }).not.toThrow();
  });
});
```

Create `apps/client/src/components/builder/__tests__/FooterPresetSlot.test.tsx` with this exact content:

```typescript
// FooterPresetSlot component tests — Phase 8 TDD RED stubs
// RED: Cannot find module '../FooterPresetSlot' — component is created in Plan 08-04.
// These tests turn GREEN after Plan 08-04 executes.
import React from 'react';
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { FooterPresetSlot } from '../FooterPresetSlot';

function makeWrapper() {
  const client = new QueryClient({
    defaultOptions: { queries: { retry: false, staleTime: Infinity } },
  });
  return function Wrapper({ children }: { children: React.ReactNode }) {
    return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
  };
}

describe('FooterPresetSlot (HF-02)', () => {
  it('renders "No footer selected" when presetId is empty string', () => {
    const { getByText } = render(<FooterPresetSlot presetId="" />, { wrapper: makeWrapper() });
    expect(getByText('No footer selected')).toBeInTheDocument();
  });

  it('renders "No footer selected" when presetId is unknown (no matching preset fetched)', () => {
    const { getByText } = render(<FooterPresetSlot presetId="unknown-xyz" />, { wrapper: makeWrapper() });
    expect(getByText('No footer selected')).toBeInTheDocument();
  });

  it('accepts presetId prop without throwing', () => {
    expect(() => {
      render(<FooterPresetSlot presetId="footer-simple-links" />, { wrapper: makeWrapper() });
    }).not.toThrow();
  });
});
```
  </action>
  <verify>
    <automated>cd apps/client && pnpm test --reporter=verbose --run 2>&1 | Select-String -Pattern "FAIL|Cannot find module|HeaderPresetSlot|FooterPresetSlot" | Select-Object -First 20</automated>
  </verify>
  <done>
    Both test files exist. Running `pnpm test` reports them as FAILED with "Cannot find module
    '../HeaderPresetSlot'" and "Cannot find module '../FooterPresetSlot'" — not syntax errors.
    All previously passing tests (105) remain GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PresetSelector.test.tsx and add RED store action tests</name>
  <files>
    apps/client/src/components/builder/__tests__/PresetSelector.test.tsx
    apps/client/src/store/__tests__/useNewsletterStore.test.ts
  </files>
  <action>
Create `apps/client/src/components/builder/__tests__/PresetSelector.test.tsx` with this exact content:

```typescript
// PresetSelector dialog tests — Phase 8 TDD RED stubs
// RED: Cannot find module '../PresetSelector' — component is created in Plan 08-05.
// These tests turn GREEN after Plan 08-05 executes.
import React from 'react';
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { PresetSelector } from '../PresetSelector';

function makeWrapper() {
  const client = new QueryClient({
    defaultOptions: { queries: { retry: false, staleTime: Infinity } },
  });
  return function Wrapper({ children }: { children: React.ReactNode }) {
    return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
  };
}

describe('PresetSelector (HF-01, HF-02)', () => {
  it('renders a dialog element when open=true', () => {
    const onOpenChange = vi.fn();
    const onSelect = vi.fn();
    render(
      <PresetSelector
        type="header"
        currentPresetId=""
        open={true}
        onOpenChange={onOpenChange}
        onSelect={onSelect}
      />,
      { wrapper: makeWrapper() }
    );
    expect(screen.getByRole('dialog')).toBeInTheDocument();
  });

  it('does not render dialog content when open=false', () => {
    const onOpenChange = vi.fn();
    const onSelect = vi.fn();
    render(
      <PresetSelector
        type="header"
        currentPresetId=""
        open={false}
        onOpenChange={onOpenChange}
        onSelect={onSelect}
      />,
      { wrapper: makeWrapper() }
    );
    expect(screen.queryByRole('dialog')).not.toBeInTheDocument();
  });

  it('accepts type="footer" without throwing', () => {
    expect(() => {
      render(
        <PresetSelector
          type="footer"
          currentPresetId="footer-simple-links"
          open={false}
          onOpenChange={vi.fn()}
          onSelect={vi.fn()}
        />,
        { wrapper: makeWrapper() }
      );
    }).not.toThrow();
  });

  it('calls onOpenChange(false) when Escape is pressed inside the dialog', () => {
    const onOpenChange = vi.fn();
    render(
      <PresetSelector
        type="header"
        currentPresetId=""
        open={true}
        onOpenChange={onOpenChange}
        onSelect={vi.fn()}
      />,
      { wrapper: makeWrapper() }
    );
    const dialog = screen.getByRole('dialog');
    fireEvent.keyDown(dialog, { key: 'Escape', code: 'Escape' });
    expect(onOpenChange).toHaveBeenCalledWith(false);
  });
});
```

Append the following block to the END of `apps/client/src/store/__tests__/useNewsletterStore.test.ts`
(after the last closing brace of the last describe block):

```typescript

describe('useNewsletterStore — header/footer/preHeader actions (Phase 8 RED)', () => {
  // RED until Plan 08-03: updateHeader/updateFooter/updatePreHeader do not exist yet.
  // TypeError: updateHeader is not a function — correct RED signal.

  it('HF-01: updateHeader sets doc.header.presetId', () => {
    const { updateHeader } = useNewsletterStore.getState() as any;
    updateHeader('header-minimal-logo');
    const doc = useNewsletterStore.getState().doc!;
    expect(doc.header.presetId).toBe('header-minimal-logo');
  });

  it('HF-02: updateFooter sets doc.footer.presetId', () => {
    const { updateFooter } = useNewsletterStore.getState() as any;
    updateFooter('footer-simple-links');
    const doc = useNewsletterStore.getState().doc!;
    expect(doc.footer.presetId).toBe('footer-simple-links');
  });

  it('HF-04: updatePreHeader sets doc.preHeader string', () => {
    const { updatePreHeader } = useNewsletterStore.getState() as any;
    updatePreHeader('Hello inbox!');
    const doc = useNewsletterStore.getState().doc!;
    expect((doc as any).preHeader).toBe('Hello inbox!');
  });
});
```
  </action>
  <verify>
    <automated>cd apps/client && pnpm test --reporter=verbose --run 2>&1 | Select-String -Pattern "FAIL|Cannot find module|PresetSelector|updateHeader|updateFooter|updatePreHeader" | Select-Object -First 20</automated>
  </verify>
  <done>
    PresetSelector.test.tsx fails with "Cannot find module '../PresetSelector'".
    The 3 new store tests fail with "TypeError: updateHeader is not a function" (or similar).
    All previously passing 105 tests remain GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → module system | Vitest resolves imports; missing modules cause RED failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Tampering | test files | accept | Test files are dev-only artifacts; no production exposure |
</threat_model>

<verification>
- `apps/client/src/components/builder/__tests__/HeaderPresetSlot.test.tsx` exists with 4 tests
- `apps/client/src/components/builder/__tests__/FooterPresetSlot.test.tsx` exists with 3 tests
- `apps/client/src/components/builder/__tests__/PresetSelector.test.tsx` exists with 4 tests
- `apps/client/src/store/__tests__/useNewsletterStore.test.ts` has a new describe block with 3 tests
- Running `pnpm --filter nl-layouter-client test --run` shows the new tests FAIL (correct RED signal)
- No previously passing tests regress
</verification>

<success_criteria>
14 new tests exist in 4 files; all 14 are RED for the correct reason (module not found or TypeError for missing actions); all 105 pre-existing tests remain GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/08-header-footer-presets-and-pre-header/08-00-SUMMARY.md`
</output>
