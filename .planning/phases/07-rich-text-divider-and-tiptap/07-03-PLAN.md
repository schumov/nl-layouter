---
phase: 07-rich-text-divider-and-tiptap
plan: "07-03"
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - apps/client/src/store/useNewsletterStore.ts
  - apps/client/src/store/__tests__/useNewsletterStore.test.ts
autonomous: true
requirements:
  - ELEM-06
  - ELEM-09

must_haves:
  truths:
    - "addElement('slotId', 'rich-text') creates element with content: { type: 'doc', content: [{ type: 'paragraph' }] }"
    - "addElement('slotId', 'rich-text') creates element with textStyle: 'body'"
    - "addElement('slotId', 'divider') creates element with color '#cccccc', spacing 16, thickness 1"
    - "2 new store tests are GREEN"
    - "All 84 pre-existing tests remain GREEN"
  artifacts:
    - path: "apps/client/src/store/useNewsletterStore.ts"
      provides: "Updated createDefaultElement with correct rich-text initial content"
      contains: "type: 'paragraph'"
    - path: "apps/client/src/store/__tests__/useNewsletterStore.test.ts"
      provides: "Store unit tests for rich-text and divider addElement defaults"
      contains: "addElement"
  key_links:
    - from: "apps/client/src/store/useNewsletterStore.ts"
      to: "apps/client/src/components/builder/RichTextEditor.tsx"
      via: "addElement creates TiptapJSONDoc that RichTextEditor initializes with"
      pattern: "rich-text"
---

<objective>
Update `createDefaultElement` in `useNewsletterStore.ts` so that the rich-text default content includes an empty paragraph node (not an empty array), and add store unit tests verifying both rich-text and divider defaults.

Purpose: D-12 specifies `content: { type: 'doc', content: [{ type: 'paragraph' }] }` for new rich-text elements. The current store has `content: []` which is technically valid but produces an empty editor with no cursor. TipTap expects at least one paragraph node to be present on initialization. The divider default is already correct — only rich-text needs updating.

Output: 1 line changed in `useNewsletterStore.ts`; 1 new store test file with 2 tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-rich-text-divider-and-tiptap/07-CONTEXT.md
@apps/client/src/store/useNewsletterStore.ts
@apps/client/src/types/newsletter.ts
@apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx

<interfaces>
<!-- Relevant section from useNewsletterStore.ts — createDefaultElement function to modify -->
```typescript
// Current (needs update — 'rich-text' case only):
case 'rich-text':
  return { type: 'rich-text', id, content: { type: 'doc', content: [] }, textStyle: 'body' };

// Required (D-12 — empty paragraph so TipTap has a cursor position):
case 'rich-text':
  return {
    type: 'rich-text',
    id,
    content: { type: 'doc', content: [{ type: 'paragraph' }] },
    textStyle: 'body',
  };

// Divider (already correct — DO NOT change):
case 'divider':
  return { type: 'divider', id, color: '#cccccc', spacing: 16, thickness: 1 };
```

<!-- How ColumnSlot.test.tsx sets up store state for reference (test pattern to follow): -->
```typescript
useNewsletterStore.setState({ doc: structuredClone(FIXTURE_DOC), selectedElementId: null });
(useNewsletterStore.getState() as any).addElement('fixture-slot-1col-1', 'image');
const slot = useNewsletterStore.getState().doc!.rows[0].slots[0];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update rich-text default content and add store tests</name>
  <files>
    apps/client/src/store/useNewsletterStore.ts,
    apps/client/src/store/__tests__/useNewsletterStore.test.ts
  </files>
  <behavior>
    - Test: `addElement(slotId, 'rich-text')` → slot.element.content equals `{ type: 'doc', content: [{ type: 'paragraph' }] }`
    - Test: `addElement(slotId, 'divider')` → slot.element has `{ color: '#cccccc', spacing: 16, thickness: 1 }`
  </behavior>
  <action>
**Step 1 — Edit `apps/client/src/store/useNewsletterStore.ts`:**

Find the `'rich-text'` case in `createDefaultElement` (currently around line 36). Change the single line:

```typescript
// FROM:
return { type: 'rich-text', id, content: { type: 'doc', content: [] }, textStyle: 'body' };

// TO:
return {
  type: 'rich-text',
  id,
  content: { type: 'doc', content: [{ type: 'paragraph' }] },
  textStyle: 'body',
};
```

Only this one case changes. The `'divider'` case is already correct and must NOT be modified.

**Step 2 — Create `apps/client/src/store/__tests__/useNewsletterStore.test.ts`:**

Create the `__tests__` directory under `apps/client/src/store/` if it doesn't exist.

```typescript
// apps/client/src/store/__tests__/useNewsletterStore.test.ts
//
// Unit tests for useNewsletterStore addElement defaults (D-12).
// These tests use direct Zustand state manipulation — same pattern as ColumnSlot.test.tsx.

import { describe, it, expect, beforeEach } from 'vitest';
import { useNewsletterStore } from '../useNewsletterStore';
import { FIXTURE_DOC } from '../../fixtures/newsletter.fixture';
import type { RichTextElement, DividerElement } from '../../types/newsletter';

beforeEach(() => {
  // Reset store to a clean fixture doc before each test
  useNewsletterStore.setState({
    doc: structuredClone(FIXTURE_DOC),
    selectedElementId: null,
  });
});

describe('useNewsletterStore — addElement defaults (D-12)', () => {
  it('ELEM-06: rich-text default has content with one empty paragraph node', () => {
    useNewsletterStore.getState().addElement('fixture-slot-1col-1', 'rich-text');
    const slot = useNewsletterStore.getState().doc!.rows[0].slots[0];
    const element = slot.element as RichTextElement;
    expect(element.type).toBe('rich-text');
    expect(element.textStyle).toBe('body');
    expect(element.content).toEqual({
      type: 'doc',
      content: [{ type: 'paragraph' }],
    });
  });

  it('ELEM-09: divider default has color #cccccc, spacing 16, thickness 1', () => {
    useNewsletterStore.getState().addElement('fixture-slot-1col-1', 'divider');
    const slot = useNewsletterStore.getState().doc!.rows[0].slots[0];
    const element = slot.element as DividerElement;
    expect(element.type).toBe('divider');
    expect(element.color).toBe('#cccccc');
    expect(element.spacing).toBe(16);
    expect(element.thickness).toBe(1);
  });
});
```

**Note on fixture slot ID:** `'fixture-slot-1col-1'` is the slot ID used in `ColumnSlot.test.tsx` — it is guaranteed to exist in `FIXTURE_DOC`. This test seeds the fixture, calls `addElement`, then reads the slot back via `doc.rows[0].slots[0]` (the first slot of the first section).
  </action>
  <verify>
    <automated>pnpm --filter nl-layouter-client test -- src/store/__tests__/useNewsletterStore.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
Both store tests GREEN. `useNewsletterStore.ts` compiles without TypeScript errors. The change to `createDefaultElement` does not break any of the 84 pre-existing tests (verify with full suite: `pnpm --filter nl-layouter-client test`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| addElement action → Zustand store | Creates new element; default content is hardcoded, not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | createDefaultElement | accept | Default values are hardcoded constants; no user input path in this function |
</threat_model>

<verification>
- `pnpm --filter nl-layouter-client test -- src/store/__tests__/useNewsletterStore.test.ts` → 2 tests passing
- `pnpm --filter nl-layouter-client test` → 84 + 2 = 86 passing (plus 16 still-RED from Plan 07-00)
- `grep "type: 'paragraph'" apps/client/src/store/useNewsletterStore.ts` returns 1 match in the rich-text case
- The divider case line `color: '#cccccc', spacing: 16, thickness: 1` is unchanged
</verification>

<success_criteria>
2 new store tests passing. `createDefaultElement('rich-text')` produces `content: { type: 'doc', content: [{ type: 'paragraph' }] }`. Full suite at 86+ passing with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rich-text-divider-and-tiptap/07-03-SUMMARY.md`
</output>
