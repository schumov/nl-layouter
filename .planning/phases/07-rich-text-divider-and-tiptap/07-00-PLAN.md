---
phase: 07-rich-text-divider-and-tiptap
plan: "07-00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/client/src/components/builder/__tests__/DividerRenderer.test.tsx
  - apps/client/src/components/builder/__tests__/DividerEditor.test.tsx
  - apps/client/src/components/builder/__tests__/RichTextStaticRenderer.test.tsx
  - apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx
autonomous: true
requirements:
  - ELEM-06
  - ELEM-07
  - ELEM-08
  - ELEM-09

must_haves:
  truths:
    - "DividerRenderer.test.tsx exists with 5 failing tests (Cannot find module)"
    - "DividerEditor.test.tsx exists with 5 failing tests (Cannot find module)"
    - "RichTextStaticRenderer.test.tsx exists with 4 failing tests (Cannot find module)"
    - "InspectorPanel.test.tsx Phase 7 tests are RED (assertion fails — placeholder still rendered)"
    - "The 4 pre-Phase-7 InspectorPanel tests remain GREEN after this plan"
  artifacts:
    - path: "apps/client/src/components/builder/__tests__/DividerRenderer.test.tsx"
      provides: "RED test contract for DividerRenderer"
      contains: "DividerRenderer"
    - path: "apps/client/src/components/builder/__tests__/DividerEditor.test.tsx"
      provides: "RED test contract for DividerEditor"
      contains: "DividerEditor"
    - path: "apps/client/src/components/builder/__tests__/RichTextStaticRenderer.test.tsx"
      provides: "RED test contract for RichTextStaticRenderer"
      contains: "RichTextStaticRenderer"
    - path: "apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx"
      provides: "Updated InspectorPanel tests with Phase 7 assertions"
      contains: "Phase 7"
  key_links:
    - from: "apps/client/src/components/builder/__tests__/DividerRenderer.test.tsx"
      to: "apps/client/src/components/builder/DividerRenderer.tsx"
      via: "import { DividerRenderer } from '../DividerRenderer'"
      pattern: "DividerRenderer"
    - from: "apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx"
      to: "apps/client/src/components/builder/InspectorPanel.tsx"
      via: "import { InspectorPanel } from '../InspectorPanel'"
      pattern: "InspectorPanel"
---

<objective>
Create the Phase 7 RED test scaffold. Every new test file imports from modules that do not yet exist, or asserts on behavior the current code does not yet provide. Tests must fail for the right reason — import error or assertion failure — NOT for syntax errors.

Purpose: Lock the exact behavioral contracts that Plans 07-01 through 07-06 must satisfy. No implementation file is created in this plan.

Output: 3 new test files + 1 updated test file. ~16 new RED tests. All 84 pre-existing passing tests remain GREEN.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-rich-text-divider-and-tiptap/07-CONTEXT.md
@apps/client/src/types/newsletter.ts
@apps/client/src/components/builder/__tests__/ButtonEditor.test.tsx
@apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx

<interfaces>
<!-- Types that test fixtures must conform to — from newsletter.ts -->
```typescript
export interface DividerElement {
  type: 'divider';
  id: string;
  color: string;    // hex e.g. "#cccccc"
  spacing: number;  // vertical padding px — applies top AND bottom
  thickness: number; // hr height px
}

export interface RichTextElement {
  type: 'rich-text';
  id: string;
  content: TiptapJSONDoc; // { type: 'doc', content: TiptapNode[] }
  textStyle: 'header' | 'subheader' | 'body' | 'code';
}

export interface TiptapJSONDoc {
  type: 'doc';
  content: TiptapNode[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DividerRenderer.test.tsx and DividerEditor.test.tsx (RED)</name>
  <files>
    apps/client/src/components/builder/__tests__/DividerRenderer.test.tsx,
    apps/client/src/components/builder/__tests__/DividerEditor.test.tsx
  </files>
  <action>
Create two NEW test files. Both import from modules that do not exist yet — they will fail with "Cannot find module". Do NOT create any implementation files.

**File: apps/client/src/components/builder/__tests__/DividerRenderer.test.tsx**

```tsx
import React from 'react';
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { DividerRenderer } from '../DividerRenderer';
import type { DividerElement } from '../../../types/newsletter';

function makeDivider(overrides: Partial<DividerElement> = {}): DividerElement {
  return { type: 'divider', id: 'div-1', color: '#cccccc', spacing: 16, thickness: 1, ...overrides };
}

describe('DividerRenderer (ELEM-09)', () => {
  it('renders an <hr> element', () => {
    const { container } = render(<DividerRenderer element={makeDivider()} />);
    expect(container.querySelector('hr')).not.toBeNull();
  });

  it('applies border-top color from element.color', () => {
    const { container } = render(<DividerRenderer element={makeDivider({ color: '#ff0000' })} />);
    const hr = container.querySelector('hr') as HTMLElement;
    expect(hr.style.borderTop).toContain('#ff0000');
  });

  it('applies border-top thickness from element.thickness', () => {
    const { container } = render(<DividerRenderer element={makeDivider({ thickness: 3 })} />);
    const hr = container.querySelector('hr') as HTMLElement;
    expect(hr.style.borderTop).toContain('3px');
  });

  it('applies vertical padding from element.spacing', () => {
    const { container } = render(<DividerRenderer element={makeDivider({ spacing: 24 })} />);
    const wrapper = container.firstChild as HTMLElement;
    expect(wrapper.style.padding).toContain('24px');
  });

  it('renders without errors using default props', () => {
    expect(() => render(<DividerRenderer element={makeDivider()} />)).not.toThrow();
  });
});
```

**File: apps/client/src/components/builder/__tests__/DividerEditor.test.tsx**

```tsx
import React from 'react';
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { DividerEditor } from '../DividerEditor';
import type { DividerElement } from '../../../types/newsletter';

const MOCK_DIVIDER: DividerElement = {
  type: 'divider', id: 'div-1', color: '#cccccc', spacing: 16, thickness: 1,
};

describe('DividerEditor (ELEM-09)', () => {
  it('color picker dispatches onUpdate with { color }', () => {
    const onUpdate = vi.fn();
    render(<DividerEditor element={MOCK_DIVIDER} onUpdate={onUpdate} />);
    // Find the native color swatch (type="color") among inputs showing '#cccccc'
    const colorInputs = screen.getAllByDisplayValue('#cccccc');
    const swatch = colorInputs.find(
      (el) => (el as HTMLInputElement).type === 'color',
    ) as HTMLInputElement;
    fireEvent.change(swatch, { target: { value: '#ff0000' } });
    expect(onUpdate).toHaveBeenCalledWith({ color: '#ff0000' });
  });

  it('hex text input dispatches onUpdate with { color } for valid 7-char hex', () => {
    const onUpdate = vi.fn();
    render(<DividerEditor element={MOCK_DIVIDER} onUpdate={onUpdate} />);
    const colorInputs = screen.getAllByDisplayValue('#cccccc');
    const hexText = colorInputs.find(
      (el) => (el as HTMLInputElement).type === 'text',
    ) as HTMLInputElement;
    fireEvent.change(hexText, { target: { value: '#abcdef' } });
    expect(onUpdate).toHaveBeenCalledWith({ color: '#abcdef' });
  });

  it('thickness range slider dispatches onUpdate with { thickness } as a number', () => {
    const onUpdate = vi.fn();
    render(<DividerEditor element={MOCK_DIVIDER} onUpdate={onUpdate} />);
    const slider = screen.getByRole('slider');
    fireEvent.change(slider, { target: { value: '4' } });
    expect(onUpdate).toHaveBeenCalledWith({ thickness: 4 });
  });

  it('spacing number input dispatches onUpdate with { spacing } as a number', () => {
    const onUpdate = vi.fn();
    render(<DividerEditor element={MOCK_DIVIDER} onUpdate={onUpdate} />);
    // The spacing number input shows value 16; the only input with that display value
    const spacingInput = screen.getByDisplayValue('16');
    fireEvent.change(spacingInput, { target: { value: '32' } });
    expect(onUpdate).toHaveBeenCalledWith({ spacing: 32 });
  });

  it('renders section label text for Color, Thickness, and Spacing', () => {
    render(<DividerEditor element={MOCK_DIVIDER} onUpdate={vi.fn()} />);
    expect(screen.getByText('Color')).toBeInTheDocument();
    expect(screen.getByText('Thickness')).toBeInTheDocument();
    expect(screen.getByText('Spacing')).toBeInTheDocument();
  });
});
```
  </action>
  <verify>
    <automated>pnpm --filter nl-layouter-client test -- src/components/builder/__tests__/DividerRenderer.test.tsx src/components/builder/__tests__/DividerEditor.test.tsx 2>&1 | Select-String -Pattern "FAIL|Cannot find module"</automated>
  </verify>
  <done>Both files exist; both test runs fail with "Cannot find module '../DividerRenderer'" and "Cannot find module '../DividerEditor'" respectively. No syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create RichTextStaticRenderer.test.tsx and update InspectorPanel.test.tsx for Phase 7</name>
  <files>
    apps/client/src/components/builder/__tests__/RichTextStaticRenderer.test.tsx,
    apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx
  </files>
  <action>
**CREATE (new file): apps/client/src/components/builder/__tests__/RichTextStaticRenderer.test.tsx**

```tsx
import React from 'react';
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { RichTextStaticRenderer } from '../RichTextStaticRenderer';
import type { RichTextElement } from '../../../types/newsletter';

function makeRichText(overrides: Partial<RichTextElement> = {}): RichTextElement {
  return {
    type: 'rich-text',
    id: 'rt-1',
    content: {
      type: 'doc',
      content: [{ type: 'paragraph', content: [{ type: 'text', text: 'Hello world' }] }],
    },
    textStyle: 'body',
    ...overrides,
  };
}

describe('RichTextStaticRenderer (ELEM-06, ELEM-07, ELEM-08)', () => {
  it('renders text content from TipTap JSON doc', () => {
    const { container } = render(<RichTextStaticRenderer element={makeRichText()} />);
    expect(container.textContent).toContain('Hello world');
  });

  it('applies header preset wrapper style: fontSize 28px, fontWeight 700', () => {
    const { container } = render(
      <RichTextStaticRenderer element={makeRichText({ textStyle: 'header' })} />,
    );
    const div = container.firstChild as HTMLElement;
    expect(div.style.fontSize).toBe('28px');
    expect(div.style.fontWeight).toBe('700');
  });

  it('applies body preset wrapper style: fontSize 16px', () => {
    const { container } = render(
      <RichTextStaticRenderer element={makeRichText({ textStyle: 'body' })} />,
    );
    const div = container.firstChild as HTMLElement;
    expect(div.style.fontSize).toBe('16px');
  });

  it('renders paragraph HTML via dangerouslySetInnerHTML', () => {
    const { container } = render(
      <RichTextStaticRenderer
        element={makeRichText({
          content: {
            type: 'doc',
            content: [{ type: 'paragraph', content: [{ type: 'text', text: 'Test' }] }],
          },
        })}
      />,
    );
    // generateHTML produces <p>...</p>; dangerouslySetInnerHTML injects it into the wrapper div
    expect(container.innerHTML).toMatch(/<p[^>]*>.*Test.*<\/p>/);
  });
});
```

**MODIFY: apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx**

Make exactly these three changes to the existing file:

**Change A:** Add TipTap mocks at the top level (after the import block, before the fixtures). These are hoisted by Vitest and are no-ops until Phase 7 wires the real editors:

```tsx
// TipTap mocks — needed once InspectorPanel renders RichTextEditor (Plan 07-06)
vi.mock('@tiptap/react', () => ({
  useEditor: vi.fn(() => null),
  EditorContent: () => <div data-testid="editor-content" />,
}));
vi.mock('@tiptap/react/menus', () => ({
  BubbleMenu: () => null,
}));
```

**Change B:** REMOVE the following test (it asserts the Phase 7 placeholder text that Plan 07-06 will replace):
```
it('D-08: rich-text element shows Phase 7 note in body', () => {
  ...
  expect(screen.getByText('Editor available in Phase 7.')).toBeInTheDocument();
});
```

**Change C:** ADD a new describe block at the bottom of the file:

```tsx
describe('InspectorPanel (Phase 7 — RED until Plan 07-06)', () => {
  it('ELEM-09: renders DividerEditor controls (Thickness label) when element is divider type', () => {
    render(<InspectorPanel element={MOCK_DIVIDER} onBack={() => {}} onUpdate={vi.fn()} />);
    expect(screen.getByText('Thickness')).toBeInTheDocument();
  });

  it('ELEM-06: renders RichTextEditor preset picker (Header button) when element is rich-text type', () => {
    render(<InspectorPanel element={MOCK_RICHTEXT} onBack={() => {}} onUpdate={vi.fn()} />);
    expect(screen.getByRole('button', { name: 'Header' })).toBeInTheDocument();
  });
});
```

These 2 new tests will be RED (InspectorPanel still shows "Editor available in Phase 7." placeholder) until Plan 07-06 replaces the placeholder with real components.
  </action>
  <verify>
    <automated>pnpm --filter nl-layouter-client test -- src/components/builder/__tests__/RichTextStaticRenderer.test.tsx src/components/builder/__tests__/InspectorPanel.test.tsx 2>&1 | Select-String -Pattern "PASS|FAIL|✓|×|pass|fail"</automated>
  </verify>
  <done>
RichTextStaticRenderer.test.tsx: all 4 tests RED ("Cannot find module '../RichTextStaticRenderer'").
InspectorPanel.test.tsx: 4 pre-existing tests PASS; 2 new Phase 7 tests FAIL with assertion errors (not import errors — InspectorPanel itself still renders the placeholder text). The removed "Phase 7 note" test is gone from the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → test files | Test files are developer artifacts only; no runtime security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Tampering | test fixtures | accept | Test fixtures are hardcoded developer data; no user input path |
</threat_model>

<verification>
- `pnpm --filter nl-layouter-client test` shows exactly 84 tests passing (unchanged from Phase 6)
- 16 new tests failing across the 4 files (5 + 5 + 4 + 2)
- All failures are "Cannot find module" or assertion failures — not syntax errors or TypeScript errors
- The `vi.mock('@tiptap/react')` block is present in InspectorPanel.test.tsx
- The old "Editor available in Phase 7." test is absent from InspectorPanel.test.tsx
</verification>

<success_criteria>
Running `pnpm --filter nl-layouter-client test` shows 84 previously-passing tests still GREEN and ~16 new tests RED with correct failure messages.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rich-text-divider-and-tiptap/07-00-SUMMARY.md`
</output>
