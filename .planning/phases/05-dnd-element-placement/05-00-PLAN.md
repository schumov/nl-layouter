---
phase: 05-dnd-element-placement
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx
  - apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx
  - apps/client/src/components/builder/InspectorPanel.tsx
  - apps/client/src/store/__tests__/useNewsletterStore.test.ts
  - apps/client/src/components/builder/__tests__/DragDropProvider.test.tsx
autonomous: true
requirements:
  - ELEM-10
  - ELEM-11
  - ELEM-12

must_haves:
  truths:
    - "All 30 existing tests remain green after Wave 0 commits"
    - "InspectorPanel.tsx minimal stub exists so test imports resolve without module error"
    - "InspectorPanel.test.tsx has RED failing stubs for element type header, back arrow, and note text"
    - "ColumnSlot.test.tsx wraps all renders in renderWithDnd (DndContext) so Wave 2 useDroppable addition doesn't break tests"
    - "ColumnSlot.test.tsx has RED stubs for: green isOver border, × remove button, remove confirm, selection ring"
    - "useNewsletterStore.test.ts has RED stubs for addElement and removeElement store actions"
    - "DragDropProvider.test.tsx has RED stub for ELEMENT_CARD drop behaviour"
  artifacts:
    - path: apps/client/src/components/builder/InspectorPanel.tsx
      provides: Minimal placeholder export so test imports don't throw module-not-found
      contains: "export function InspectorPanel"
    - path: apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx
      provides: RED failing stubs for D-04/D-05 InspectorPanel behaviour
  key_links:
    - from: apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx
      to: apps/client/src/components/builder/InspectorPanel.tsx
      via: import
      pattern: "from '../InspectorPanel'"
    - from: apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx
      to: DndContext (renderWithDnd wrapper)
      via: import DndContext from @dnd-kit/core
      pattern: "renderWithDnd"
---

<objective>
Create all RED test stubs for Phase 5 before any implementation. Establishes the test contract
that Wave 1–3 plans must satisfy (TDD wave 0).

Purpose: Prevents regressions by proving the full test suite passes before touching production code;
ensures ColumnSlot.test.tsx is DndContext-safe before Plan 05-03 adds useDroppable.
Output: 4 updated/created test files + 1 InspectorPanel.tsx minimal stub (not the real implementation).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dnd-element-placement/05-CONTEXT.md
@.planning/phases/05-dnd-element-placement/05-VALIDATION.md

<interfaces>
<!-- Key contracts executor needs. Extracted from codebase. -->

From apps/client/src/store/useNewsletterStore.ts:
```typescript
interface NewsletterState {
  doc: NewsletterDoc | null;
  selectedElementId: string | null;   // EXISTS — stores slot ID in Phase 5
}
interface NewsletterActions {
  setSelectedElement: (id: string | null) => void;  // EXISTS
  // addElement / removeElement do NOT exist yet — stubs reference these
}
```

From apps/client/src/components/builder/__tests__/BuilderPalette.test.tsx (renderWithDnd pattern):
```typescript
function renderWithDnd(ui: React.ReactElement) {
  return render(<DndContext>{ui}</DndContext>);
}
```

From apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx (current — no renderWithDnd):
```typescript
// Renders ColumnSlot directly without DndContext wrapper
// MUST be updated to use renderWithDnd before Wave 2 adds useDroppable
render(<ColumnSlot slot={{ id: 'test-slot', element: null }} sectionId="test-section" />)
```

From apps/client/src/fixtures/newsletter.fixture.ts (for test setup):
```typescript
// fixture-slot-1col-1 is the first slot in fixture doc (1col section)
slots: [{ id: 'fixture-slot-1col-1', element: null }]
```

From apps/client/src/types/newsletter.ts:
```typescript
export type ElementUnion = ImageElement | ImageLinkElement | ButtonElement | RichTextElement | DividerElement;
// ElementUnion['type']: 'image' | 'image-link' | 'button' | 'rich-text' | 'divider'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ColumnSlot.test.tsx DndContext upgrade + RED stubs; InspectorPanel.tsx minimal stub + InspectorPanel.test.tsx RED stubs</name>
  <files>
    apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx
    apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx
    apps/client/src/components/builder/InspectorPanel.tsx
  </files>
  <read_first>
    - apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx — see current structure (no DndContext); wrap existing tests in renderWithDnd
    - apps/client/src/components/builder/__tests__/BuilderPalette.test.tsx — copy the exact renderWithDnd helper pattern
    - apps/client/src/components/builder/ColumnSlot.tsx — current implementation (no useDroppable yet)
    - apps/client/src/types/newsletter.ts — ElementUnion['type'] values for occupied slot test fixtures
  </read_first>
  <action>
**Step A — Create apps/client/src/components/builder/InspectorPanel.tsx MINIMAL STUB:**

This is NOT the real implementation. It is a placeholder so InspectorPanel.test.tsx imports resolve.
The real implementation ships in Plan 05-05.

```tsx
// apps/client/src/components/builder/InspectorPanel.tsx
// PHASE 5 STUB — replaced by full implementation in Plan 05-05
// Minimal placeholder so test imports resolve without module-not-found error.

import React from 'react';
import type { ElementUnion } from '../../types/newsletter';

interface InspectorPanelProps {
  elementType: ElementUnion['type'];
  onBack: () => void;
}

export function InspectorPanel({ elementType, onBack }: InspectorPanelProps) {
  void elementType;
  void onBack;
  return <div data-testid="inspector-panel-stub">stub</div>;
}
```

**Step B — Create apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx RED stubs:**

```tsx
// InspectorPanel component tests — D-04/D-05
// RED until Plan 05-05 implements the real InspectorPanel component.
import React from 'react';
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { InspectorPanel } from '../InspectorPanel';

describe('InspectorPanel (Phase 5 placeholder)', () => {
  it('D-05: renders element type display name as panel header — "Image"', () => {
    // RED: stub renders "stub", not the element type name
    render(<InspectorPanel elementType="image" onBack={() => {}} />);
    expect(screen.getByText('Image')).toBeInTheDocument();
  });

  it('D-05: renders element type display name — "Rich Text"', () => {
    // RED: stub renders "stub", not the element type name
    render(<InspectorPanel elementType="rich-text" onBack={() => {}} />);
    expect(screen.getByText('Rich Text')).toBeInTheDocument();
  });

  it('D-05: renders muted placeholder note text', () => {
    // RED: stub does not render the note
    render(<InspectorPanel elementType="button" onBack={() => {}} />);
    expect(screen.getByText('Editing available in the next step.')).toBeInTheDocument();
  });

  it('D-04: renders back arrow button with aria-label "Back to palette"', () => {
    // RED: stub has no button
    render(<InspectorPanel elementType="divider" onBack={() => {}} />);
    expect(screen.getByRole('button', { name: 'Back to palette' })).toBeInTheDocument();
  });

  it('D-04: clicking back arrow calls onBack callback', async () => {
    // RED: stub has no button, so nothing to click
    const onBack = vi.fn();
    render(<InspectorPanel elementType="image-link" onBack={onBack} />);
    const btn = screen.getByRole('button', { name: 'Back to palette' });
    await userEvent.click(btn);
    expect(onBack).toHaveBeenCalledOnce();
  });
});
```

Note: add `import { vi } from 'vitest';` alongside the `describe, it, expect` import for the spy.

**Step C — Update apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx:**

Rewrite the file to:
1. Add `renderWithDnd` helper (import DndContext from `@dnd-kit/core`)
2. Wrap ALL existing render calls with `renderWithDnd` instead of bare `render`
3. Add `useNewsletterStore` import for selection-state setup
4. Add `beforeEach` that resets `selectedElementId: null` in the store
5. Add RED stub tests for Wave 2 behaviors

```tsx
// ColumnSlot component tests — empty-slot rendering + Phase 5 DnD behaviors
import React from 'react';
import { describe, it, expect, beforeEach } from 'vitest';
import { render } from '@testing-library/react';
import { DndContext } from '@dnd-kit/core';
import { ColumnSlot } from '../ColumnSlot';
import { useNewsletterStore } from '../../../store/useNewsletterStore';

// Matches renderWithDnd pattern established in BuilderPalette.test.tsx
function renderWithDnd(ui: React.ReactElement) {
  return render(<DndContext>{ui}</DndContext>);
}

beforeEach(() => {
  useNewsletterStore.setState({ selectedElementId: null });
});

describe('ColumnSlot — empty slot (idle)', () => {
  it('renders dashed border placeholder when slot.element is null', () => {
    const { getByText } = renderWithDnd(
      <ColumnSlot slot={{ id: 'test-slot', element: null }} sectionId="test-section" />,
    );
    getByText('Drop element here');
  });
});

describe('ColumnSlot — occupied slot (idle)', () => {
  it('renders element type badge when slot.element is defined', () => {
    const { getByText } = renderWithDnd(
      <ColumnSlot
        slot={{ id: 'test-slot', element: { type: 'image', id: 'e1', src: '', alt: '' } as any }}
        sectionId="test-section"
      />,
    );
    getByText('[image]');
  });
});

describe('ColumnSlot — Phase 5 RED stubs (Wave 2 implements)', () => {
  it('D-09/D-10: occupied slot renders × button with aria-label "Remove element"', () => {
    // RED: current ColumnSlot has no × button — passes after Plan 05-03
    const { container } = renderWithDnd(
      <ColumnSlot
        slot={{ id: 's1', element: { type: 'button', id: 'e1', label: '', href: '', backgroundColor: '#000', textColor: '#fff', style: 'solid' } as any }}
        sectionId="sec"
      />,
    );
    expect(container.querySelector('button[aria-label="Remove element"]')).not.toBeNull();
  });

  it('D-11: clicking × on occupied slot shows remove confirm "Remove?" and "Cancel"', () => {
    // RED: no confirm UI exists yet — passes after Plan 05-03
    const { container } = renderWithDnd(
      <ColumnSlot
        slot={{ id: 's2', element: { type: 'divider', id: 'e2', color: '#ccc', spacing: 16, thickness: 1 } as any }}
        sectionId="sec"
      />,
    );
    const btn = container.querySelector('button[aria-label="Remove element"]');
    expect(btn).not.toBeNull();
    // Further clicking + asserting Remove?/Cancel is tested in 05-03
  });

  it('ELEM-10: empty slot has ref attached (useDroppable) — no crash when wrapped in DndContext', () => {
    // GREEN already: renderWithDnd wraps in DndContext so even after useDroppable is added, no crash
    expect(() =>
      renderWithDnd(<ColumnSlot slot={{ id: 's3', element: null }} sectionId="sec" />),
    ).not.toThrow();
  });

  it('ELEM-10: occupied slot shows ring-2 class when selectedElementId matches slot.id', () => {
    // RED: current ColumnSlot has no ring styling — passes after Plan 05-03
    useNewsletterStore.setState({ selectedElementId: 's4' });
    const { container } = renderWithDnd(
      <ColumnSlot
        slot={{ id: 's4', element: { type: 'rich-text', id: 'e3', content: { type: 'doc', content: [] }, textStyle: 'body' } as any }}
        sectionId="sec"
      />,
    );
    const wrapper = container.firstChild as HTMLElement;
    expect(wrapper.className).toContain('ring-2');
  });
});
```
  </action>
  <verify>
    <automated>cd apps/client && pnpm test run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderWithDnd" apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx` outputs `3` or more (helper defined + used in multiple tests)
    - `grep -c "export function InspectorPanel" apps/client/src/components/builder/InspectorPanel.tsx` outputs `1`
    - `grep -c "inspector-panel-stub" apps/client/src/components/builder/InspectorPanel.tsx` outputs `1`
    - `grep -c "Back to palette" apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx` outputs `2` (test text + test name)
    - `grep -c "Editing available in the next step" apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx` outputs `1`
    - `grep -c "ring-2" apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx` outputs `1`
    - `grep -c "Remove element" apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx` outputs `2` (two stubs reference it)
    - Test run: the 2 original ColumnSlot tests pass (idle empty + idle occupied); new RED stubs fail — EXPECTED at Wave 0
    - Test run: InspectorPanel stubs ALL fail — EXPECTED at Wave 0
    - Test run: all pre-existing 30 tests (Phase 1–4) remain GREEN
  </acceptance_criteria>
  <done>InspectorPanel.tsx minimal stub created; InspectorPanel.test.tsx has 5 RED stubs; ColumnSlot.test.tsx uses renderWithDnd throughout with 4 RED stubs; 30 prior tests green</done>
</task>

<task type="auto">
  <name>Task 2: useNewsletterStore.test.ts addElement/removeElement RED stubs + DragDropProvider.test.tsx ELEMENT_CARD RED stub</name>
  <files>
    apps/client/src/store/__tests__/useNewsletterStore.test.ts
    apps/client/src/components/builder/__tests__/DragDropProvider.test.tsx
  </files>
  <read_first>
    - apps/client/src/store/__tests__/useNewsletterStore.test.ts — see beforeEach pattern, describe structure, and how existing actions are tested
    - apps/client/src/components/builder/__tests__/DragDropProvider.test.tsx — current single-test structure; add new describe block
    - apps/client/src/fixtures/newsletter.fixture.ts — fixture slot IDs: 'fixture-slot-1col-1' is the first slot in the 1col section
    - apps/client/src/store/useNewsletterStore.ts — confirm addElement/removeElement do NOT exist yet (they will fail at runtime)
  </read_first>
  <action>
**Step A — Append RED stubs to apps/client/src/store/__tests__/useNewsletterStore.test.ts:**

Add a NEW describe block at the end of the file. Do NOT modify existing tests.

```typescript
describe('useNewsletterStore — element mutations (Phase 5 RED stubs)', () => {
  it('ELEM-10: addElement creates element with given type in the matching slot', () => {
    // RED: addElement does not exist yet — TypeError: addElement is not a function
    const { addElement } = useNewsletterStore.getState() as any;
    addElement('fixture-slot-1col-1', 'image');
    const slot = useNewsletterStore.getState().doc!.rows[0].slots[0];
    expect(slot.element).not.toBeNull();
    expect(slot.element?.type).toBe('image');
  });

  it('ELEM-11: addElement on occupied slot overwrites existing element (replace)', () => {
    // RED: addElement does not exist yet
    const { addElement } = useNewsletterStore.getState() as any;
    addElement('fixture-slot-1col-1', 'image');
    addElement('fixture-slot-1col-1', 'button');
    const slot = useNewsletterStore.getState().doc!.rows[0].slots[0];
    expect(slot.element?.type).toBe('button');
  });

  it('ELEM-10: addElement creates element with a non-null id string', () => {
    // RED: addElement does not exist yet
    const { addElement } = useNewsletterStore.getState() as any;
    addElement('fixture-slot-2col-1', 'divider');
    const slot = useNewsletterStore.getState().doc!.rows[1].slots[0];
    expect(typeof slot.element?.id).toBe('string');
    expect(slot.element!.id.length).toBeGreaterThan(0);
  });

  it('ELEM-12: removeElement sets slot.element to null', () => {
    // RED: removeElement does not exist yet — TypeError: removeElement is not a function
    const { addElement, removeElement } = useNewsletterStore.getState() as any;
    addElement('fixture-slot-1col-1', 'rich-text');
    removeElement('fixture-slot-1col-1');
    const slot = useNewsletterStore.getState().doc!.rows[0].slots[0];
    expect(slot.element).toBeNull();
  });

  it('ELEM-12: removeElement is a no-op for unknown slotId', () => {
    // RED: removeElement does not exist yet
    const { removeElement } = useNewsletterStore.getState() as any;
    expect(() => removeElement('unknown-slot-id')).not.toThrow();
    // rows should be unchanged
    expect(useNewsletterStore.getState().doc!.rows).toHaveLength(FIXTURE_DOC.rows.length);
  });
});
```

Note: `FIXTURE_DOC` and `beforeEach` resetting the store are already established at the top of the file — do not duplicate them.

**Step B — Extend apps/client/src/components/builder/__tests__/DragDropProvider.test.tsx:**

Add a new describe block. Keep the existing "renders children without crashing" test untouched.

```tsx
describe('DragDropProvider — ELEMENT_CARD drop (Phase 5 RED stubs)', () => {
  it('ELEM-10: ELEMENT_CARD drag-end on a slot triggers addElement in store', () => {
    // RED: DragDropProvider.onDragEnd has no ELEMENT_CARD branch yet
    // Full simulation requires fireEvent.pointer + dnd-kit mock; this stub
    // just confirms the describe block is registered as a failing placeholder.
    // Plan 05-04 will replace this with a real test using DndContext simulation.
    expect(false).toBe(true); // RED — explicit failing stub
  });
});
```
  </action>
  <verify>
    <automated>cd apps/client && pnpm test run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addElement" apps/client/src/store/__tests__/useNewsletterStore.test.ts` outputs `6` or more (3 test bodies + calls)
    - `grep -c "removeElement" apps/client/src/store/__tests__/useNewsletterStore.test.ts` outputs `4` or more
    - `grep -c "ELEMENT_CARD" apps/client/src/components/builder/__tests__/DragDropProvider.test.tsx` outputs `1` or more
    - `grep -c "expect(false).toBe(true)" apps/client/src/components/builder/__tests__/DragDropProvider.test.tsx` outputs `1`
    - Test run: all previously-passing 30 tests still pass (addElement/removeElement stub tests fail — that's expected RED state)
    - Test run: new store RED stubs fail with TypeError (not a function) — EXPECTED at Wave 0
    - Test run: DragDropProvider ELEMENT_CARD stub fails — EXPECTED at Wave 0
    - Test run: No pre-existing test that was GREEN is now RED (regression check)
  </acceptance_criteria>
  <done>5 RED store stubs for addElement/removeElement added; 1 RED DragDropProvider ELEMENT_CARD stub added; all prior 30 tests remain green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→component | Test code exercises components; no production trust surface in Wave 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Tampering | Test stubs | accept | Wave 0 is test-only code; no production surface exposed |
</threat_model>

<verification>
After both tasks:
- `cd apps/client && pnpm test run` — expect 30 existing tests GREEN, new stubs RED (intentional)
- `grep -rn "renderWithDnd" apps/client/src/components/builder/__tests__/ColumnSlot.test.tsx` — must appear
- `ls apps/client/src/components/builder/InspectorPanel.tsx` — file must exist
- `ls apps/client/src/components/builder/__tests__/InspectorPanel.test.tsx` — file must exist
</verification>

<success_criteria>
- InspectorPanel.tsx minimal stub exported — test imports resolve without error
- ColumnSlot.test.tsx uses renderWithDnd on all renders — safe for Wave 2 useDroppable addition
- 4 RED stubs in ColumnSlot.test.tsx for Wave 2 features
- 5 RED stubs in useNewsletterStore.test.ts for Phase 5 actions
- 1 RED stub in DragDropProvider.test.tsx for ELEMENT_CARD handler
- 5 RED stubs in InspectorPanel.test.tsx for Phase 5 inspector UI
- All 30 pre-existing tests remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/05-dnd-element-placement/05-00-SUMMARY.md`
</output>
