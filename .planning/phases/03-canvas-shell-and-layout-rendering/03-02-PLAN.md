---
plan_id: 03-02
phase: 3
wave: 1
depends_on:
  - 03-00
files_modified:
  - apps/client/src/components/ui/tabs.tsx
  - apps/client/src/components/builder/BuilderPalette.tsx
  - apps/client/src/components/builder/__tests__/BuilderPalette.test.tsx
requirements:
  - CANVAS-01
autonomous: true
must_haves:
  truths:
    - "shadcn Tabs component is installed at apps/client/src/components/ui/tabs.tsx via CLI (not hand-rolled)"
    - "BuilderPalette renders as flex-[2] right panel with border-l"
    - "BuilderPalette shows two tab triggers: 'Layouts' and 'Elements'"
    - "Layouts tab content lists exactly 5 layout cards with labels: '1 Column', '2 Columns', '3 Columns', 'Small-Left / Big-Right', 'Big-Left / Small-Right'"
    - "Elements tab content shows stub text 'Elements will be available in a future phase.'"
    - "TabsList has rounded-none and border-b classes (flush against panel top, no gap)"
  artifacts:
    - path: "apps/client/src/components/ui/tabs.tsx"
      provides: "Radix UI Tabs primitives via shadcn"
      exports: ["Tabs", "TabsList", "TabsTrigger", "TabsContent"]
    - path: "apps/client/src/components/builder/BuilderPalette.tsx"
      provides: "right panel palette with layout cards"
      exports: ["BuilderPalette"]
  key_links:
    - from: "apps/client/src/components/builder/BuilderPalette.tsx"
      to: "apps/client/src/components/ui/tabs.tsx"
      via: "import { Tabs, TabsContent, TabsList, TabsTrigger }"
      pattern: "from '@/components/ui/tabs'"
    - from: "BuilderPalette LAYOUT_NAMES"
      to: "five layout cards"
      via: "Object.entries().map()"
      pattern: "LAYOUT_NAMES"
---

<objective>
Install the shadcn `tabs` component via CLI and create `BuilderPalette.tsx` — the right panel of the two-panel builder layout containing the Layouts/Elements tab group.

Purpose: Delivers the palette half of CANVAS-01. BuilderPalette is independent of the rendering chain (ColumnGrid, RowBlock) so it runs in parallel with Plan 03-01.
Output: `apps/client/src/components/ui/tabs.tsx` (generated by shadcn CLI) and `BuilderPalette.tsx` with 5 layout cards and Elements stub.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canvas-shell-and-layout-rendering/03-UI-SPEC.md
@.planning/phases/03-canvas-shell-and-layout-rendering/03-PATTERNS.md

<interfaces>
<!-- shadcn Tabs usage pattern (confirmed from RESEARCH.md and PATTERNS.md) -->
```typescript
// Import path after CLI install:
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';

// Usage pattern:
<Tabs defaultValue="layouts" className="h-full flex flex-col">
  <TabsList className="w-full shrink-0 rounded-none border-b">
    <TabsTrigger value="layouts" className="flex-1">Layouts</TabsTrigger>
    <TabsTrigger value="elements" className="flex-1">Elements</TabsTrigger>
  </TabsList>
  <TabsContent value="layouts" className="p-4 space-y-2">
    {/* layout cards */}
  </TabsContent>
  <TabsContent value="elements" className="p-4">
    {/* stub */}
  </TabsContent>
</Tabs>
```

<!-- BuilderPalette wrapper (from UI-SPEC and PATTERNS.md) -->
```typescript
// Outer div — matches BuilderCanvas's flex-[3] sibling:
<div className="flex-[2] min-w-0 border-l bg-background overflow-y-auto">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn tabs + create BuilderPalette</name>
  <files>
    apps/client/src/components/ui/tabs.tsx,
    apps/client/src/components/builder/BuilderPalette.tsx,
    apps/client/src/components/builder/__tests__/BuilderPalette.test.tsx
  </files>

  <read_first>
    - `apps/client/src/components/ui/button.tsx` — read lines 1–10 for shadcn component import structure and `data-slot` attribute convention; tabs.tsx will follow the same pattern
    - `apps/client/components.json` — confirm shadcn config (style, aliases) to ensure CLI uses correct settings
    - `apps/client/src/components/builder/BuilderHeader.tsx` — read lines 1–5 for React import convention (`import React from 'react'`) and named export pattern
  </read_first>

  <action>
**Step 1 — Install shadcn tabs component:**
Run this command from the `apps/client` directory:
```bash
npx shadcn@latest add tabs
```
This generates `apps/client/src/components/ui/tabs.tsx` using the existing `components.json` config (new-york style, neutral baseColor, css-variables). The `@radix-ui/react-tabs@1.1.14` package is already in the pnpm virtual store as a transitive dep — no new npm package is downloaded.

Do NOT hand-roll this file. If the CLI asks for confirmation, type `y`.

**Step 2 — Create `apps/client/src/components/builder/BuilderPalette.tsx`:**
```typescript
import React from 'react';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';

// Display labels for the 5 layout types (matches UI-SPEC copywriting contract)
const LAYOUT_NAMES: Record<string, string> = {
  '1col':                 '1 Column',
  '2col':                 '2 Columns',
  '3col':                 '3 Columns',
  'small-left-big-right': 'Small-Left / Big-Right',
  'big-left-small-right': 'Big-Left / Small-Right',
};

export function BuilderPalette() {
  return (
    <div className="flex-[2] min-w-0 border-l bg-background overflow-y-auto">
      <Tabs defaultValue="layouts" className="h-full flex flex-col">
        <TabsList className="w-full shrink-0 rounded-none border-b">
          <TabsTrigger value="layouts" className="flex-1">Layouts</TabsTrigger>
          <TabsTrigger value="elements" className="flex-1">Elements</TabsTrigger>
        </TabsList>
        <TabsContent value="layouts" className="p-4 space-y-2">
          {Object.entries(LAYOUT_NAMES).map(([type, label]) => (
            <div key={type} className="p-3 border rounded-md text-sm cursor-default">
              {label}
            </div>
          ))}
        </TabsContent>
        <TabsContent value="elements" className="p-4">
          <p className="text-sm text-muted-foreground">
            Elements will be available in a future phase.
          </p>
        </TabsContent>
      </Tabs>
    </div>
  );
}
```

Key class requirements (verified against UI-SPEC):
- Outer div: `flex-[2] min-w-0 border-l bg-background overflow-y-auto`
- `TabsList`: `w-full shrink-0 rounded-none border-b` — `rounded-none` overrides shadcn default radius for flush top; `shrink-0` prevents tab bar shrinking; `border-b` separator
- Each `TabsTrigger`: `flex-1` — makes both tabs equal width
- Layout cards: `p-3 border rounded-md text-sm cursor-default` — `cursor-default` because cards are non-interactive in Phase 3
- Layout card labels MUST use exact UI-SPEC copywriting: `1 Column`, `2 Columns`, `3 Columns`, `Small-Left / Big-Right`, `Big-Left / Small-Right`
- Elements stub: `text-sm text-muted-foreground`

**Step 3 — Fill `apps/client/src/components/builder/__tests__/BuilderPalette.test.tsx`:**
Replace the entire file content with:
```typescript
// BuilderPalette component tests — CANVAS-01
import React from 'react';
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { BuilderPalette } from '../BuilderPalette';

describe('BuilderPalette', () => {
  it('renders Layouts tab and Elements tab', () => {
    const { getByText } = render(<BuilderPalette />);
    getByText('Layouts');
    getByText('Elements');
  });

  it('renders all 5 layout cards in Layouts tab', () => {
    const { getByText } = render(<BuilderPalette />);
    getByText('1 Column');
    getByText('2 Columns');
    getByText('3 Columns');
    getByText('Small-Left / Big-Right');
    getByText('Big-Left / Small-Right');
  });

  it('renders Elements stub text in Elements tab', () => {
    const { getByText } = render(<BuilderPalette />);
    getByText('Elements will be available in a future phase.');
  });
});
```
Note: shadcn Tabs uses Radix UI which renders all tab content in the DOM (accessibility requirement). The `getByText` queries work without needing to click tabs — all content is present in the initial render.
  </action>

  <verify>
    <automated>pnpm --filter ./apps/client test --run BuilderPalette && pnpm --filter ./apps/client exec tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `apps/client/src/components/ui/tabs.tsx` exists and contains `TabsList` (grep: `grep "TabsList" apps/client/src/components/ui/tabs.tsx`)
    - `apps/client/src/components/ui/tabs.tsx` was CLI-generated — contains `data-slot` attribute (grep: `grep "data-slot" apps/client/src/components/ui/tabs.tsx`)
    - `apps/client/src/components/builder/BuilderPalette.tsx` exists and contains `flex-[2]` (grep: `grep "flex-\[2\]" apps/client/src/components/builder/BuilderPalette.tsx`)
    - `BuilderPalette.tsx` contains `rounded-none` and `border-b` and `shrink-0` on TabsList (grep: `grep "rounded-none" apps/client/src/components/builder/BuilderPalette.tsx`)
    - `BuilderPalette.tsx` contains all 5 exact layout labels — `1 Column`, `2 Columns`, `3 Columns`, `Small-Left / Big-Right`, `Big-Left / Small-Right` (grep each)
    - `BuilderPalette.tsx` contains `Elements will be available in a future phase.` (grep: `grep "future phase" apps/client/src/components/builder/BuilderPalette.tsx`)
    - `BuilderPalette.tsx` contains `cursor-default` on layout cards (grep: `grep "cursor-default" apps/client/src/components/builder/BuilderPalette.tsx`)
    - `pnpm --filter ./apps/client test --run BuilderPalette` shows 3 tests passing
    - `pnpm --filter ./apps/client exec tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>shadcn tabs installed via CLI; BuilderPalette created with 5 layout cards and Elements stub; BuilderPalette tests pass; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | BuilderPalette is a static display component with no user input; all content is hardcoded strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | LAYOUT_NAMES record | accept | Module-level const; no user input affects this data; layout names are hardcoded string literals |
| T-03-02-02 | Information Disclosure | shadcn tabs CLI generation | accept | shadcn official registry only; no third-party registry used; component is local file generation, no network dependency at runtime |
</threat_model>

<verification>
After Plan 03-02 completes:

```bash
pnpm --filter ./apps/client test --run BuilderPalette && pnpm --filter ./apps/client exec tsc --noEmit
```

Expected: 3 BuilderPalette tests pass, TypeScript exits 0.

Visual check (after Wave 2 wires BuilderPage): palette shows two tabs; "Layouts" tab lists 5 layout cards; "Elements" tab shows stub text.
</verification>

<success_criteria>
- `apps/client/src/components/ui/tabs.tsx` exists, was generated by shadcn CLI, exports `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`
- `apps/client/src/components/builder/BuilderPalette.tsx` exports `BuilderPalette` as a named function
- Palette outer div has `flex-[2] min-w-0 border-l bg-background overflow-y-auto`
- `TabsList` has `w-full shrink-0 rounded-none border-b`
- Layouts tab contains exactly 5 cards with labels matching UI-SPEC copywriting contract
- Elements tab contains stub paragraph with exact text "Elements will be available in a future phase."
- All 3 BuilderPalette tests pass (`renders Layouts tab and Elements tab`, `renders all 5 layout cards`, `renders Elements stub text`)
- TypeScript compilation exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-canvas-shell-and-layout-rendering/03-02-SUMMARY.md`
</output>
