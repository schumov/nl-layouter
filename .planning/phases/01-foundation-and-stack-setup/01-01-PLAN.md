---
phase: 01-foundation-and-stack-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .gitignore
  - apps/client/package.json
  - apps/client/tsconfig.json
  - apps/client/tsconfig.app.json
  - apps/client/tsconfig.node.json
  - apps/client/vite.config.ts
  - apps/client/index.html
  - apps/client/src/main.tsx
  - apps/server/package.json
  - apps/server/tsconfig.json
autonomous: true
requirements: []  # Infrastructure phase — no v1 requirements mapped to Phase 1

must_haves:
  truths:
    - "pnpm is available via `pnpm --version` in any terminal session"
    - "pnpm workspace recognizes both apps (`pnpm ls -r` at root lists client and server)"
    - "apps/client/package.json lists all required client dependencies at the verified versions"
    - "apps/server/package.json lists all required server dependencies at the verified versions"
    - "tsconfig.base.json has `strict: true` and is extended by both app tsconfigs"
    - "node_modules are installed for both apps after pnpm install"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definition"
      contains: "apps/*"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript strict config"
      contains: "\"strict\": true"
    - path: "apps/client/package.json"
      provides: "Client dependencies spec"
      contains: "@tiptap/react"
    - path: "apps/server/package.json"
      provides: "Server dependencies spec"
      contains: "fastify"
    - path: "apps/client/tsconfig.app.json"
      provides: "Client TS config extending base"
      contains: "../../tsconfig.base.json"
    - path: "apps/server/tsconfig.json"
      provides: "Server TS config extending base"
      contains: "../../tsconfig.base.json"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "apps/client, apps/server"
      via: "packages glob"
      pattern: "apps/\\*"
    - from: "apps/client/tsconfig.app.json"
      to: "../../tsconfig.base.json"
      via: "extends"
      pattern: "\"extends\""
    - from: "apps/server/tsconfig.json"
      to: "../../tsconfig.base.json"
      via: "extends"
      pattern: "\"extends\""
---

<objective>
Create the pnpm monorepo workspace skeleton with both app directories scaffolded, all dependencies declared in package.json files at verified exact versions, and shared TypeScript strict configuration in place.

Purpose: Every subsequent plan depends on this structure. If dependencies are wrong or tsconfig is missing, no other plan can succeed.
Output: Installable workspace — `pnpm install` from root installs all deps for both apps.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/phases/01-foundation-and-stack-setup/01-RESEARCH.md

<!-- Key patterns to use: Pattern 1 (pnpm workspace), Pattern 2 (tsconfig.base.json) from 01-RESEARCH.md -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pnpm + create root workspace files</name>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, .gitignore</files>

  <read_first>
    - .planning/phases/01-foundation-and-stack-setup/01-RESEARCH.md (Pattern 1: pnpm workspace setup, Pattern 2: tsconfig.base.json)
  </read_first>

  <action>
**Step 1 — Install pnpm globally (MUST run before anything else):**
```powershell
npm install -g pnpm
pnpm --version   # must print a version, e.g. "10.x.x"
```

**Step 2 — Create `pnpm-workspace.yaml`:**
```yaml
packages:
  - 'apps/*'
```

**Step 3 — Create root `package.json`:**
```json
{
  "name": "nl-layouter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently --names client,server --prefix-colors cyan,yellow \"pnpm --filter ./apps/client dev\" \"pnpm --filter ./apps/server dev\"",
    "build": "pnpm --recursive run build",
    "typecheck": "pnpm --recursive run typecheck"
  },
  "devDependencies": {
    "concurrently": "10.0.3",
    "typescript": "6.0.3"
  }
}
```

**Step 4 — Create `tsconfig.base.json`:**
```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true
  }
}
```
NOTE: If third-party types conflict with `exactOptionalPropertyTypes` or `noUncheckedIndexedAccess` later, they can be disabled per-app. Leave enabled here.

**Step 5 — Create root `.gitignore`:**
```
# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/

# Environment (NEVER commit real secrets)
.env
.env.local
.env.*.local
apps/server/.env

# Drizzle generated migrations
apps/server/drizzle/

# OS / editor
.DS_Store
*.local
.vscode/settings.json

# TypeScript
*.tsbuildinfo
```
  </action>

  <verify>
    <automated>pnpm --version</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --version` returns a version string (not an error)
    - `pnpm-workspace.yaml` contains exactly `apps/*` in packages array
    - `tsconfig.base.json` contains `"strict": true`
    - `tsconfig.base.json` contains `"noUnusedLocals": true`
    - `.gitignore` contains line `apps/server/.env`
    - `.gitignore` contains line `node_modules/`
  </acceptance_criteria>

  <done>pnpm is installed globally, workspace config exists, strict TypeScript base config exists, .gitignore excludes .env and node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold apps/client with all dependencies</name>
  <files>apps/client/package.json, apps/client/tsconfig.json, apps/client/tsconfig.app.json, apps/client/tsconfig.node.json, apps/client/vite.config.ts, apps/client/index.html, apps/client/src/main.tsx</files>

  <read_first>
    - .planning/phases/01-foundation-and-stack-setup/01-RESEARCH.md (Standard Stack — Core Client table, Pattern 2 tsconfig, Pattern 3 vite.config placeholder)
    - .planning/research/STACK.md (Stack Versions table for exact versions)
  </read_first>

  <action>
Create `apps/client/` directory and the following files:

**`apps/client/package.json`** — ALL client dependencies at verified exact versions:
```json
{
  "name": "nl-layouter-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "react-router": "7.17.0",
    "zustand": "5.0.14",
    "immer": "11.1.8",
    "@tanstack/react-query": "5.101.0",
    "@dnd-kit/core": "6.3.1",
    "@dnd-kit/sortable": "10.0.0",
    "@dnd-kit/utilities": "3.2.2",
    "@dnd-kit/modifiers": "9.0.0",
    "@tiptap/react": "3.26.0",
    "@tiptap/pm": "3.26.0",
    "@tiptap/starter-kit": "3.26.0",
    "@tiptap/extensions": "3.26.0",
    "@tiptap/extension-text-style": "3.26.0",
    "@tiptap/extension-color": "3.26.0",
    "lucide-react": "1.17.0",
    "clsx": "2.1.1",
    "tailwind-merge": "3.6.0",
    "class-variance-authority": "0.7.1",
    "tailwindcss": "4.3.0",
    "@tailwindcss/vite": "4.3.0"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vite": "8.0.16",
    "@vitejs/plugin-react": "6.0.2",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "vitest": "^3.0.0"
  }
}
```

**`apps/client/tsconfig.json`** — references pattern (shadcn/ui Vite convention):
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

**`apps/client/tsconfig.app.json`** — extends base, adds DOM lib and JSX:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
```

**`apps/client/tsconfig.node.json`** — for vite.config.ts:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "noEmit": true
  },
  "include": ["vite.config.ts"]
}
```

**`apps/client/vite.config.ts`** — minimal placeholder (Tailwind plugin added in Plan 01-06):
```typescript
import path from 'path'
import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 3000,
  },
})
```
NOTE: `@tailwindcss/vite` is intentionally NOT added here — Plan 01-06 adds it when configuring Tailwind v4.

**`apps/client/index.html`** — Vite entry HTML:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NL Layouter</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**`apps/client/src/main.tsx`** — minimal placeholder (fleshed out in Plan 01-06):
```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>NL Layouter — loading...</div>
  </React.StrictMode>
)
```
  </action>

  <verify>
    <automated>Test-Path apps/client/package.json</automated>
  </verify>

  <acceptance_criteria>
    - `apps/client/package.json` contains `"@tiptap/react": "3.26.0"`
    - `apps/client/package.json` contains `"@dnd-kit/core": "6.3.1"`
    - `apps/client/package.json` contains `"tailwindcss": "4.3.0"`
    - `apps/client/package.json` contains `"vite": "8.0.16"`
    - `apps/client/package.json` contains `"scripts"` with `"test": "vitest"` and `"typecheck": "tsc --noEmit"`
    - `apps/client/tsconfig.app.json` contains `"../../tsconfig.base.json"`
    - `apps/client/tsconfig.app.json` contains `"@/*": ["./src/*"]`
    - `apps/client/vite.config.ts` contains `port: 3000`
    - `apps/client/index.html` contains `src="/src/main.tsx"`
    - `apps/client/src/main.tsx` contains `getElementById('root')`
  </acceptance_criteria>

  <done>apps/client has all required files with all dependencies at exact verified versions</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold apps/server + run pnpm install</name>
  <files>apps/server/package.json, apps/server/tsconfig.json</files>

  <read_first>
    - .planning/phases/01-foundation-and-stack-setup/01-RESEARCH.md (Standard Stack — Core Server table, Verified Installation Command server)
    - .planning/research/STACK.md (Backend section for exact versions)
  </read_first>

  <action>
Create `apps/server/` directory with `apps/server/src/` subdirectory and the following files:

**`apps/server/package.json`** — ALL server dependencies at verified exact versions:
```json
{
  "name": "nl-layouter-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "5.8.5",
    "@fastify/cors": "11.2.0",
    "@fastify/cookie": "11.0.2",
    "zod": "4.4.3",
    "dotenv": "^16.0.0",
    "drizzle-orm": "0.45.2",
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "drizzle-kit": "0.31.10",
    "tsx": "4.22.4",
    "@types/node": "^22.0.0"
  }
}
```

**`apps/server/tsconfig.json`** — extends base, Node.js settings:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src", "drizzle.config.ts"]
}
```
NOTE: `drizzle.config.ts` (added in Plan 01-03) is included so `tsc --noEmit` also type-checks it.

**Run pnpm install from workspace root:**
```powershell
pnpm install
```
This installs ALL workspace dependencies for both apps in one command. Expected output includes packages being added for both `nl-layouter-client` and `nl-layouter-server`.
  </action>

  <verify>
    <automated>pnpm --filter ./apps/server list fastify</automated>
  </verify>

  <acceptance_criteria>
    - `apps/server/package.json` contains `"fastify": "5.8.5"`
    - `apps/server/package.json` contains `"drizzle-orm": "0.45.2"`
    - `apps/server/package.json` contains `"drizzle-kit": "0.31.10"` in devDependencies
    - `apps/server/package.json` contains `"scripts"` with `"dev": "tsx watch src/index.ts"`
    - `apps/server/package.json` contains `"typecheck": "tsc --noEmit"`
    - `apps/server/tsconfig.json` contains `"../../tsconfig.base.json"`
    - `apps/server/tsconfig.json` contains `"drizzle.config.ts"` in include
    - `apps/client/node_modules/react` directory exists (pnpm install completed)
    - `apps/server/node_modules/fastify` directory exists (pnpm install completed)
  </acceptance_criteria>

  <done>apps/server has all required files, pnpm install succeeded, node_modules exist for both apps</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root → git | Files committed to git must not include real secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore | mitigate | `.gitignore` explicitly excludes `.env` and `apps/server/.env` so DATABASE_URL is never committed |
| T-01-02 | Tampering | pnpm-workspace.yaml | accept | Workspace file is read-only config with no secrets; low risk |
| T-01-03 | Elevation of Privilege | package.json deps | accept | All packages installed from npm registry at pinned versions; supply chain risk is low for these well-known packages |
</threat_model>

<verification>
After completing all 3 tasks, verify the full scaffold:

```powershell
# pnpm is available
pnpm --version

# Workspace recognizes both apps
pnpm ls -r --depth 0

# Both package.json files have correct content
Select-String "@tiptap/react" apps/client/package.json
Select-String "fastify" apps/server/package.json

# Both tsconfigs extend base
Select-String "tsconfig.base.json" apps/client/tsconfig.app.json
Select-String "tsconfig.base.json" apps/server/tsconfig.json

# Dependencies installed
Test-Path apps/client/node_modules/react
Test-Path apps/server/node_modules/fastify
```
</verification>

<success_criteria>
- `pnpm --version` succeeds in any terminal
- `pnpm ls -r` at root lists both `nl-layouter-client` and `nl-layouter-server`
- `apps/client/package.json` has all 22 dependencies at exact verified versions
- `apps/server/package.json` has all 9 dependencies at exact verified versions
- `tsconfig.base.json` has `strict: true`, `noUnusedLocals: true`, `noUnusedParameters: true`
- Both app tsconfigs extend `../../tsconfig.base.json`
- `apps/client/node_modules` and `apps/server/node_modules` exist (pnpm install succeeded)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-stack-setup/01-01-SUMMARY.md`
</output>
