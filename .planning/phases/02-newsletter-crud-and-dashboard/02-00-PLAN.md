---
phase: 02-newsletter-crud-and-dashboard
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/client/vitest.config.ts
  - apps/client/src/hooks/__tests__/useAutoSave.test.ts
  - apps/client/src/hooks/__tests__/useNewsletters.test.ts
  - apps/client/src/hooks/__tests__/useDeleteNewsletter.test.ts
  - apps/client/src/components/dashboard/__tests__/CreateNewsletterDialog.test.ts
autonomous: true
requirements:
  - NL-01
  - NL-02
  - NL-05
  - NL-06

must_haves:
  truths:
    - "vitest can be invoked from apps/client and exits with code 0 (all stubs pass)"
    - "Each test stub file exists with at least one describe block and one it.todo stub"
    - "vitest.config.ts configures jsdom environment and resolves @ alias"
  artifacts:
    - path: "apps/client/vitest.config.ts"
      provides: "Vitest configuration with jsdom test environment"
      contains: "jsdom"
    - path: "apps/client/src/hooks/__tests__/useAutoSave.test.ts"
      provides: "Stub tests for auto-save hook (NL-06)"
      contains: "useAutoSave"
    - path: "apps/client/src/hooks/__tests__/useNewsletters.test.ts"
      provides: "Stub tests for newsletter query/create hooks (NL-01, NL-02)"
      contains: "useNewsletters"
    - path: "apps/client/src/hooks/__tests__/useDeleteNewsletter.test.ts"
      provides: "Stub tests for delete hook (NL-05)"
      contains: "useDeleteNewsletter"
    - path: "apps/client/src/components/dashboard/__tests__/CreateNewsletterDialog.test.ts"
      provides: "Stub tests for create dialog (NL-01)"
      contains: "CreateNewsletterDialog"
  key_links:
    - from: "apps/client/vitest.config.ts"
      to: "apps/client/src/hooks/__tests__/*.test.ts"
      via: "vitest include glob"
      pattern: "testEnvironment.*jsdom"
---

<objective>
Create the Wave 0 test infrastructure: vitest config with jsdom environment and stub test files
for all Phase 2 hooks and components. These stubs pass immediately (no assertions) and provide
the Nyquist sampling structure that Wave 1–4 plans will fill in.

Purpose: Establish automated feedback loop before any implementation code is written. Every
subsequent plan verifies with `pnpm --filter ./apps/client test --run` against these stubs.

Output: 5 files — vitest.config.ts + 4 test stub files with `it.todo` placeholders.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-newsletter-crud-and-dashboard/02-VALIDATION.md
</context>

<interfaces>
<!-- Vitest config reference — jsdom environment, @ alias, runs in apps/client -->

From apps/client/package.json (key devDeps already installed in Phase 1):
```json
{
  "devDependencies": {
    "vitest": "^3.x",
    "@testing-library/react": "^16.x",
    "@testing-library/user-event": "^14.x",
    "jsdom": "^26.x"
  }
}
```

From apps/client/vite.config.ts (@ alias definition):
```typescript
resolve: {
  alias: { '@': path.resolve(__dirname, './src') }
}
```

Test commands (from VALIDATION.md):
- Quick run: `pnpm --filter ./apps/client test --run`
- Full suite: `pnpm --recursive run test`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create vitest.config.ts with jsdom environment</name>
  <files>apps/client/vitest.config.ts</files>
  <read_first>
    - apps/client/vite.config.ts (@ alias, path setup — replicate in vitest config)
    - apps/client/package.json (verify vitest and jsdom versions are installed)
    - apps/client/tsconfig.json (paths config to mirror in vitest resolve.alias)
    - .planning/phases/02-newsletter-crud-and-dashboard/02-VALIDATION.md (test commands)
  </read_first>
  <action>
Create `apps/client/vitest.config.ts` with the following EXACT content:

```typescript
/// <reference types="vitest" />
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: [],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

Key requirements:
- `environment: 'jsdom'` — required for React component testing
- `globals: true` — allows `describe`/`it`/`expect` without imports in test files
- `resolve.alias` mirrors the `@` alias from vite.config.ts so test imports resolve correctly
- Do NOT use `include` glob — default vitest discovery finds `**/*.test.{ts,tsx}` automatically
  </action>
  <verify>
    <automated>pnpm --filter ./apps/client exec tsc --noEmit --project tsconfig.json 2>&1 | Select-String "vitest.config" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    `apps/client/vitest.config.ts` exists; contains `environment: 'jsdom'`; TypeScript does not report errors on it.
  </done>
  <acceptance_criteria>
    - File exists at `apps/client/vitest.config.ts`
    - File contains the string `environment: 'jsdom'`
    - File contains `resolve.alias` block with `'@': path.resolve`
    - File contains `globals: true`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create all test stub files</name>
  <files>
    apps/client/src/hooks/__tests__/useAutoSave.test.ts
    apps/client/src/hooks/__tests__/useNewsletters.test.ts
    apps/client/src/hooks/__tests__/useDeleteNewsletter.test.ts
    apps/client/src/components/dashboard/__tests__/CreateNewsletterDialog.test.ts
  </files>
  <read_first>
    - apps/client/vitest.config.ts (just created — confirm globals: true so no imports needed)
    - .planning/phases/02-newsletter-crud-and-dashboard/02-VALIDATION.md (exact stub requirements per row)
  </read_first>
  <action>
Create the directory structure and 4 stub test files. Create `__tests__` directories as needed.

**File 1: `apps/client/src/hooks/__tests__/useAutoSave.test.ts`**
```typescript
// Stub tests for useAutoSave hook (NL-06)
// Wave 0: placeholders — implementation fills these in Phase 2 execution
import { describe, it } from 'vitest';

describe('useAutoSave', () => {
  it.todo('does not save on initial doc load (isInitialLoadRef guard)');
  it.todo('shows "Saving…" status and fires PUT after 1500ms debounce');
  it.todo('debounces: resets timer on rapid consecutive doc changes');
  it.todo('shows "Save failed" and retries after 5 seconds on PUT error');
  it.todo('shows "Saved ✓" on success and returns to idle after 3 seconds');
  it.todo('clears all timers on unmount');
});
```

**File 2: `apps/client/src/hooks/__tests__/useNewsletters.test.ts`**
```typescript
// Stub tests for useNewsletters / useCreateNewsletter hooks (NL-01, NL-02)
// Wave 0: placeholders — implementation fills these in Phase 2 execution
import { describe, it } from 'vitest';

describe('useNewsletters', () => {
  it.todo('returns NewsletterSummary[] when GET /newsletters succeeds');
  it.todo('returns empty array when no newsletters exist');
});

describe('useCreateNewsletter', () => {
  it.todo('POSTs to /newsletters with { title } and returns { id, title }');
  it.todo('invalidates ["newsletters"] query key on success');
});
```

**File 3: `apps/client/src/hooks/__tests__/useDeleteNewsletter.test.ts`**
```typescript
// Stub tests for useDeleteNewsletter hook (NL-05)
// Wave 0: placeholders — implementation fills these in Phase 2 execution
import { describe, it } from 'vitest';

describe('useDeleteNewsletter', () => {
  it.todo('optimistically removes newsletter from ["newsletters"] cache immediately');
  it.todo('fires DELETE /newsletters/:id after 5 second undo window');
  it.todo('restores cache and shows error toast when DELETE fails');
  it.todo('cancels DELETE and restores cache when Undo is clicked within 5s');
});
```

**File 4: `apps/client/src/components/dashboard/__tests__/CreateNewsletterDialog.test.ts`**
```typescript
// Stub tests for CreateNewsletterDialog component (NL-01)
// Wave 0: placeholders — implementation fills these in Phase 2 execution
import { describe, it } from 'vitest';

describe('CreateNewsletterDialog', () => {
  it.todo('renders trigger button with label "New Newsletter"');
  it.todo('Create Newsletter button is disabled when name input is empty');
  it.todo('Create Newsletter button is enabled when name input has non-empty trimmed value');
  it.todo('calls createMutation with trimmed title on form submit');
});
```

Note: `it.todo` stubs pass immediately without running any code. The `import { describe, it } from 'vitest'` imports are needed here because `globals: true` is set in vitest.config.ts but TypeScript type checking still needs explicit imports in strict mode. Use `import { describe, it } from 'vitest'` pattern for type safety.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/client test --run 2>&1</automated>
  </verify>
  <done>
    All 4 stub files exist; `pnpm --filter ./apps/client test --run` exits with code 0; all stubs show as "todo" in output; no test failures.
  </done>
  <acceptance_criteria>
    - `apps/client/src/hooks/__tests__/useAutoSave.test.ts` exists and contains `describe('useAutoSave'`
    - `apps/client/src/hooks/__tests__/useNewsletters.test.ts` exists and contains `describe('useNewsletters'`
    - `apps/client/src/hooks/__tests__/useDeleteNewsletter.test.ts` exists and contains `describe('useDeleteNewsletter'`
    - `apps/client/src/components/dashboard/__tests__/CreateNewsletterDialog.test.ts` exists and contains `describe('CreateNewsletterDialog'`
    - `pnpm --filter ./apps/client test --run` exits with code 0 (all stubs are todo, not failing)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files → test runner | Test stubs are inert — no external calls, no runtime risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Tampering | vitest.config.ts | accept | Wave 0 is test infrastructure only; no production code touched; no user data at risk |

> Wave 0 creates only test scaffolding. No trust boundaries of significance exist in this plan.
</threat_model>

<verification>
```bash
# All stubs exist
Test-Path apps/client/vitest.config.ts
Test-Path apps/client/src/hooks/__tests__/useAutoSave.test.ts
Test-Path apps/client/src/hooks/__tests__/useNewsletters.test.ts
Test-Path apps/client/src/hooks/__tests__/useDeleteNewsletter.test.ts
Test-Path apps/client/src/components/dashboard/__tests__/CreateNewsletterDialog.test.ts

# vitest runs without errors
pnpm --filter ./apps/client test --run
```
</verification>

<success_criteria>
- `pnpm --filter ./apps/client test --run` exits code 0 with all stubs shown as todo
- All 5 files exist at the specified paths
- vitest.config.ts has `environment: 'jsdom'`
</success_criteria>

<output>
After completion, create `.planning/phases/02-newsletter-crud-and-dashboard/02-00-SUMMARY.md`
</output>
