---
phase: 02-newsletter-crud-and-dashboard
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/client/src/index.css
  - apps/client/src/components/ui/button.tsx
  - apps/client/src/components/ui/input.tsx
  - apps/client/src/components/ui/dialog.tsx
  - apps/client/src/components/ui/alert-dialog.tsx
  - apps/client/src/components/ui/dropdown-menu.tsx
  - apps/client/src/components/ui/sonner.tsx
  - apps/client/src/components/ui/card.tsx
autonomous: true
requirements:
  - NL-01
  - NL-02
  - NL-05

must_haves:
  truths:
    - "shadcn/ui components are importable from @/components/ui/* in client source"
    - "Primary buttons render in Infineon Blue (#0066cc) not near-black"
    - "Focus rings render in Infineon Blue to match brand"
    - "Sonner Toaster component is available at @/components/ui/sonner"
  artifacts:
    - path: "apps/client/src/components/ui/button.tsx"
      provides: "shadcn Button component with variant/size props"
      contains: "buttonVariants"
    - path: "apps/client/src/components/ui/sonner.tsx"
      provides: "Toaster component wrapping sonner library"
      contains: "Toaster"
    - path: "apps/client/src/index.css"
      provides: "CSS design tokens with Infineon Blue primary color"
      contains: "oklch(0.484 0.209 257.0)"
  key_links:
    - from: "apps/client/src/index.css"
      to: "shadcn Button component"
      via: "--primary CSS variable consumed by bg-primary Tailwind utility"
      pattern: "--primary: oklch\\(0\\.484"
---

<objective>
Install all required shadcn/ui components for Phase 2 and override the primary color token to
Infineon Blue. This establishes the full UI component library before any page or dialog code
is written.

Purpose: Dashboard page, Create dialog, NewsletterCard, and BuilderHeader all depend on shadcn
Button, Input, Dialog, AlertDialog, DropdownMenu, Card, and Sonner. Installing these now
(Wave 2, parallel to API routes) unblocks Wave 3+ UI work.

Output: 7 shadcn component files under `apps/client/src/components/ui/`; `index.css` updated
with Infineon Blue `--primary` and `--ring` token overrides.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-newsletter-crud-and-dashboard/02-UI-SPEC.md
@.planning/phases/02-newsletter-crud-and-dashboard/02-RESEARCH.md
</context>

<interfaces>
<!-- shadcn/ui CLI and token requirements -->

From apps/client/components.json (Phase 1 — shadcn init already ran):
```json
{
  "style": "new-york",
  "baseColor": "neutral",
  "cssVariables": true
}
```

From 02-UI-SPEC.md — Token Override Required:
```css
/* Override in apps/client/src/index.css inside :root {} */
--primary: oklch(0.484 0.209 257.0);      /* #0066cc — Infineon Blue */
--primary-foreground: oklch(0.985 0 0);   /* white text on blue */
--ring: oklch(0.484 0.209 257.0);         /* focus ring matches brand */
```

From 02-RESEARCH.md — shadcn install command:
```bash
# Run from apps/client directory
npx shadcn@latest add button input dialog alert-dialog dropdown-menu sonner card
```

From 02-UI-SPEC.md — Component inventory:
- button: variants used = default, outline, ghost (icon), destructive
- input: newsletter name field + title editor
- dialog: Create Newsletter modal
- alert-dialog: Delete confirmation
- dropdown-menu: Card ⋮ hover menu
- sonner: Undo delete toast + Export toast
- card: NewsletterCard wrapper
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components</name>
  <files>
    apps/client/src/components/ui/button.tsx
    apps/client/src/components/ui/input.tsx
    apps/client/src/components/ui/dialog.tsx
    apps/client/src/components/ui/alert-dialog.tsx
    apps/client/src/components/ui/dropdown-menu.tsx
    apps/client/src/components/ui/sonner.tsx
    apps/client/src/components/ui/card.tsx
  </files>
  <read_first>
    - apps/client/components.json (shadcn config: style=new-york, neutral base, cssVariables=true)
    - apps/client/package.json (confirm @radix-ui/* packages are already installed)
    - apps/client/src/components/ui/ (check if any ui components already exist from Phase 1)
    - .planning/phases/02-newsletter-crud-and-dashboard/02-RESEARCH.md (shadcn install section)
  </read_first>
  <action>
Run the shadcn CLI from the `apps/client` directory to install all 7 required components at once:

```bash
cd apps/client && npx shadcn@latest add button input dialog alert-dialog dropdown-menu sonner card --yes
```

The `--yes` flag accepts all prompts non-interactively (overwrites existing if any).

This generates the following files (shadcn writes them automatically):
- `apps/client/src/components/ui/button.tsx`
- `apps/client/src/components/ui/input.tsx`
- `apps/client/src/components/ui/dialog.tsx`
- `apps/client/src/components/ui/alert-dialog.tsx`
- `apps/client/src/components/ui/dropdown-menu.tsx`
- `apps/client/src/components/ui/sonner.tsx`
- `apps/client/src/components/ui/card.tsx`

Additionally, shadcn may install:
- `apps/client/src/components/ui/label.tsx` (dialog dependency)
- `apps/client/src/components/ui/separator.tsx` (dropdown-menu dependency)

Do NOT manually create these files — let the shadcn CLI generate them. The CLI reads
`components.json` (new-york style, neutral base) and generates correct component code.

If `--yes` flag is not available in the installed shadcn version, run the command without it
and confirm any prompts with 'y'.

**After install, verify** each component file exists and contains expected exports.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/client exec tsc --noEmit 2>&1</automated>
  </verify>
  <done>
    All 7 component files exist under `apps/client/src/components/ui/`; TypeScript compiles without errors.
  </done>
  <acceptance_criteria>
    - `apps/client/src/components/ui/button.tsx` exists and contains `buttonVariants`
    - `apps/client/src/components/ui/dialog.tsx` exists and contains `DialogContent`
    - `apps/client/src/components/ui/alert-dialog.tsx` exists and contains `AlertDialogContent`
    - `apps/client/src/components/ui/dropdown-menu.tsx` exists and contains `DropdownMenuContent`
    - `apps/client/src/components/ui/sonner.tsx` exists and contains `Toaster`
    - `apps/client/src/components/ui/card.tsx` exists and contains `Card`
    - `pnpm --filter ./apps/client exec tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Override primary and ring CSS tokens with Infineon Blue</name>
  <files>apps/client/src/index.css</files>
  <read_first>
    - apps/client/src/index.css (full file — identify the `:root {}` block inside `@layer base`)
    - .planning/phases/02-newsletter-crud-and-dashboard/02-UI-SPEC.md (Token Override Required section — exact values)
    - .planning/phases/02-newsletter-crud-and-dashboard/02-PATTERNS.md (index.css modification pattern)
  </read_first>
  <action>
Edit `apps/client/src/index.css` to override the `--primary` and `--ring` tokens inside the
existing `:root {}` block within `@layer base {}`.

**Find the existing lines:**
```css
--primary: oklch(0.205 0 0);
--ring: oklch(0.708 0 0);
```

**Replace them with:**
```css
--primary: oklch(0.484 0.209 257.0);      /* Infineon Blue #0066cc */
--primary-foreground: oklch(0.985 0 0);   /* white — unchanged but explicit */
--ring: oklch(0.484 0.209 257.0);         /* focus ring matches brand blue */
```

EXACT token values (from 02-UI-SPEC.md Token Override Required section):
- `--primary`: `oklch(0.484 0.209 257.0)` — this is the Infineon Blue (#0066cc equivalent in oklch)
- `--ring`: `oklch(0.484 0.209 257.0)` — same value, ensures focus rings match brand

The `--primary-foreground` token should already be `oklch(0.985 0 0)` (white) from shadcn neutral
init — confirm it is set correctly and leave unchanged if already correct.

Do NOT modify any other tokens. Do NOT modify the `@theme inline` block. Only change the two
values inside `@layer base { :root { ... } }`.

**What NOT to change:**
- `@import "tailwindcss"` at the top
- `@custom-variant dark` line
- `@theme inline` block (lines 5–41)
- Any token other than `--primary` and `--ring`
  </action>
  <verify>
    <automated>Select-String -Path "apps/client/src/index.css" -Pattern "oklch\(0\.484 0\.209 257\.0\)" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    `apps/client/src/index.css` contains `oklch(0.484 0.209 257.0)` in the `--primary` declaration;
    same value for `--ring`; count of matches is 2 (one for `--primary`, one for `--ring`).
  </done>
  <acceptance_criteria>
    - `apps/client/src/index.css` contains `--primary: oklch(0.484 0.209 257.0)`
    - `apps/client/src/index.css` contains `--ring: oklch(0.484 0.209 257.0)`
    - Both occurrences of `oklch(0.484 0.209 257.0)` appear inside the `@layer base { :root {` block
    - The string `oklch(0.205 0 0)` (old near-black primary) no longer appears in the file
    - `pnpm --filter ./apps/client exec tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn CLI → file system | shadcn CLI writes component files; these are static UI code with no secrets |
| index.css → browser | CSS token values are public; no security concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | shadcn CLI output | accept | shadcn CLI generates code from official templates; reviewed by open source community |
| T-02-02-02 | Information Disclosure | index.css | accept | CSS tokens are public styles; no sensitive data |
| T-02-02-03 | Spoofing | Infineon Blue token | accept | Token is a brand color constant, not a trust mechanism; no auth or security implication |
</threat_model>

<verification>
```bash
# shadcn components installed
Test-Path apps/client/src/components/ui/button.tsx
Test-Path apps/client/src/components/ui/sonner.tsx
Test-Path apps/client/src/components/ui/alert-dialog.tsx

# Infineon Blue token override
Select-String -Path "apps/client/src/index.css" -Pattern "--primary: oklch\(0\.484"
Select-String -Path "apps/client/src/index.css" -Pattern "--ring: oklch\(0\.484"

# TypeScript clean
pnpm --filter ./apps/client exec tsc --noEmit
```
</verification>

<success_criteria>
- All 7 shadcn component files exist under `apps/client/src/components/ui/`
- `index.css` contains `--primary: oklch(0.484 0.209 257.0)` and `--ring: oklch(0.484 0.209 257.0)`
- `pnpm --filter ./apps/client exec tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-newsletter-crud-and-dashboard/02-02-SUMMARY.md`
</output>
