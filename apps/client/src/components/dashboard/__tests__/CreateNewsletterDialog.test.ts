// Stub tests for CreateNewsletterDialog component (NL-01)
// Wave 0: placeholders — implementation fills these in Phase 2 execution
import { describe, it } from 'vitest';

describe('CreateNewsletterDialog', () => {
  it.todo('renders trigger button with label "New Newsletter"');
  it.todo('Create Newsletter button is disabled when name input is empty');
  it.todo('Create Newsletter button is enabled when name input has non-empty trimmed value');
  it.todo('calls createMutation with trimmed title on form submit');
});
